# Secrets -- never commit your real .env
# keep `.env.example` devoid of the secret values
.env

# don't persiste reminders data
data/reminders.json

# Node
node_modules/
npm-debug.log*

# OS/editor cruft
.DS_Store
