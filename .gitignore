# Secrets -- never commit your real .env
# keep `.env.example` devoid of the secret values
.env

# poll definitions (data/poll-*.json) are committed to git;
# only the per-poll results/votes and cumulative scores are ignored
data/reminders.json
data/poll-results-*.json
data/poll_*.json
data/dad-jokes-state.json

# Node
node_modules/
npm-debug.log*

# OS/editor cruft
.DS_Store
